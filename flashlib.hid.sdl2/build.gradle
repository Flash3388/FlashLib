apply from: rootProject.file('jcomponent.gradle')

dependencies {
    api project(':flashlib.core.hid')
    api castle()

    // expose it so we could have access to JNI elements
    api 'com.github.tomtzook:jsdl2:1.0.0'
}