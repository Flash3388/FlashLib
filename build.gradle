
subprojects{
	buildDir = projectOutputFolder

    repositories {
        mavenCentral()

        flatDir {
            dirs projectLibsFolder
        }
    }
}

task clean(type: Delete){
    delete rootProject.buildDir
}
