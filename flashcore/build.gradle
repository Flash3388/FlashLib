apply from: rootProject.file('jcomponent.gradle')
apply from: rootProject.file('component-benchmark.gradle')

dependencies {
    api project(':flashutil')
    api javabeans()
    api jmath()
    api castle()
}
