apply plugin: 'java'

sourceCompatibility = pSourceCompatibility

sourceSets{
	main{
		java{
			srcDir 'src'
		}
	}
}
repositories {
    flatDir {
        dirs 'libs'
    }
}
dependencies {
    compile (
			[name: 'flashlib'],
			[name: 'jsch-0154'],
			[name: 'opencv-300']
	)
}
jar {
   manifest {
        attributes (
				'Main-Class': pMainClass,
				'Class-Path': '. flashboard_lib/flashlib-0.1.0.jar flashboard_lib/jsch-0154.jar flashboard_lib/opencv-300.jar'
		)
   }
}